# Fastlane for the TestFlight CI (see .github/workflows/ios-testflight.yml).
# No Gemfile.lock is committed — it cannot be resolved on the Windows dev
# machine (no Ruby); the CI resolves gems fresh.
source "https://rubygems.org"

gem "fastlane"
