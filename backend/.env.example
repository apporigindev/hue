# Seasonist subscription backend — copy to .env and fill in.

NODE_ENV=development
PORT=8080

# Postgres (required in production; omit locally to use an in-memory store)
# DATABASE_URL=postgres://user:pass@host:5432/seasonist

# ── Apple (App Store) ─────────────────────────────────────────────────
APPLE_BUNDLE_ID=com.apporigin.seasonist
# Your numeric App Store app id (App Store Connect → App Information → Apple ID).
# Required for Production verification; may be omitted while testing in Sandbox.
# APPLE_APP_APPLE_ID=1234567890
APPLE_ENVIRONMENT=Sandbox
# Folder with the Apple Root CA certs (download the DER .cer files from
# https://www.apple.com/certificateauthority/ — at least AppleRootCA-G3.cer).
APPLE_ROOT_CERTS_DIR=./certs
APPLE_ENABLE_ONLINE_CHECKS=false

# Optional: App Store Server API key (only if you later call Apple's API).
# APPLE_ISSUER_ID=
# APPLE_KEY_ID=
# APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# ── Google Play (optional — Android) ──────────────────────────────────
# GOOGLE_PACKAGE_NAME=com.apporigin.seasonist
# base64 of your Play Developer API service-account JSON
# GOOGLE_SERVICE_ACCOUNT_B64=

# ── App auth ──────────────────────────────────────────────────────────
# Shared secret the app sends as the x-api-key header on verify/entitlement.
# APP_API_KEY=

# ── "See it for real" generative try-on ───────────────────────────────
# off (default) hides the feature; the app flags on /health.tryonAvailable.
# fal = fal.ai (needs FAL_API_KEY); mock = dev/test placeholder images only.
TRYON_PROVIDER=off
# Your fal.ai API key (https://fal.ai/dashboard/keys). Server-side ONLY —
# never ship this in the app.
# FAL_API_KEY=
# fal.ai image-EDIT model (confirm the exact id on fal.ai before launch).
TRYON_MODEL=fal-ai/flux-pro/kontext
# The consumable product that entitles a try-on pack.
TRYON_PRODUCT_ID=seasonist.tryon.unlock
# Renders per purchased pack, and the max selfie size accepted.
TRYON_MAX_COLORS=5
TRYON_MAX_IMAGE_MB=10
# Dev/test ONLY — skip purchase verification so the flow runs without StoreKit.
# Ignored in production.
# TRYON_DEV_BYPASS=false
#
# The transactionId proof path (recommended) additionally needs the App Store
# Server API credentials above (APPLE_ISSUER_ID / APPLE_KEY_ID / APPLE_PRIVATE_KEY).
